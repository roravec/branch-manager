# branch-manager
Navrhovaný systém slúži na správu pobočiek, zamestnancov a zdrojov v rámci organizácie. Prístup k funkcionalitám je riadený podľa používateľských rolí, aby bola zabezpečená bezpečnosť a integrita údajov. Cieľom systému je prehľadná administrácia, sledovanie aktivít a vizualizácia pobočiek v rámci Slovenska.

# Poziadavky
TODO: Doplnit poziadavky na spustenie ako min verzia php, druh databazy(mysql,mongodb...) a jej min verzia, pre frontend to iste.

# Instalacia
TODO: Doplnit postup na spojazdnenie, ako to nakonfigurovat (db kde sa nastavuje, ze treba spustit migraciu databazy, aku databazu to potrebuje)
```bash
git clone...
git submodule update --init --recursive --remote

```
